<%= form_for(@flight, :url => preview_flights_path, :remote => true) do |f| %>
  <% if @flight.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@flight.errors.count, "error") %> prohibited this flight from being saved:</h2>

      <ul>
      <% @flight.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>  
                                    
  <div class="field">
    <%= f.label :flight_number, "Flugnummer" %><br />
    <%= f.text_field :flight_number %>
  </div>

  <div class="actions">
    <%= link_to 'Back', flights_path, :class => "btn" %>
    <%= submit_tag "Preview", :class => "form_submit btn primary"%>
  </div>
<% end %>
